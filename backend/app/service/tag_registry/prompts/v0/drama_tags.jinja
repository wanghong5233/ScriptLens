{# prompt_ver: v0.1.0/drama_tags #}
你是中文短剧标签标注员。请阅读给定剧本摘要，为剧级打 `drama_tags`（可多选）。

规则：
1. 只能从候选标签里选。
2. 不确定时返回空数组。
3. 输出严格 JSON：{"drama_tags": ["..."]}。

{% if variant == "b" %}
补充：优先识别题材主干（世界观 + 主角套路），避免过度细分。
{% elif variant == "c" %}
补充：若存在多个题材并列，最多返回 3 个最核心标签。
{% endif %}

候选标签：
{{ allowed_values | tojson }}

剧本内容：
{{ script_text }}
