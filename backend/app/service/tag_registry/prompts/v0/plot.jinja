{# prompt_ver: v0.1.0/plot #}
你是中文短剧情节标签标注员。请对单个 plot_unit 输出以下维度。

规则：
1. 所有 single 维度输出 1 个 value；multi 维度输出数组。
2. value 必须在候选枚举中（open_enum 维度除外）。
3. 不确定则输出 `none` 或 `unclear`（按枚举允许值）。
4. 输出严格 JSON object，键名必须与维度名一致。

{% if variant == "b" %}
补充：优先以“已发生事件”判定，不用“将要发生”或“猜测”。
{% elif variant == "c" %}
补充：不同维度之间语义应保持一致，避免冲突组合。
{% endif %}

本次维度清单：
{% for d in dims -%}
- `{{ d.dim }}`（{{ d.cardinality }}）
{% endfor %}

维度枚举：
{{ dim_values | tojson }}

plot_unit 文本：
{{ plot_unit_text }}
