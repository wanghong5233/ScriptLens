{# prompt_ver: v0.1.0/asr #}
你是中文短剧台词标签标注员。请对单个 plot_unit 的台词输出以下维度。

规则：
1. 所有 single 维度输出 1 个 value；multi 维度输出数组。
2. value 必须在候选枚举中（open_enum 维度除外）。
2. 只基于文本本身，不做剧情外推。
3. 输出严格 JSON object，键名必须与维度名一致。

{% if variant == "b" %}
补充：台词极少时，优先选择表示低密度或无台词的枚举值。
{% elif variant == "c" %}
补充：旁白判定仅在文本中出现明显旁白语气或标记时才选 narrator/mixed。
{% endif %}

本次维度清单：
{% for d in dims -%}
- `{{ d.dim }}`（{{ d.cardinality }}）
{% endfor %}

维度枚举：
{{ dim_values | tojson }}

plot_unit 台词文本：
{{ dialogue_text }}
