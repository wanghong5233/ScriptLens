{# prompt_ver: v1.0.0/relationship #}
你是短剧关系分析标注员。请对角色关系对输出本次维度结果。

规则：
1. 所有 single 维度输出 1 个 value；multi 维度输出数组。
2. 必须从候选枚举中选择（open_enum 维度除外）。
3. 输出严格 JSON object，键名必须与维度名一致。

{% if variant == "b" %}
补充：`relationship_dynamic_arc` 关注跨剧情段的主导演化，不看单场波动。
{% elif variant == "c" %}
补充：`relationship_triangle` 仅在明确三角/多角结构成立时才标注，默认 none。
{% endif %}

本次维度清单：
{% for d in dims -%}
- `{{ d.dim }}`（{{ d.cardinality }}）
{% endfor %}

维度枚举：
{{ dim_values | tojson }}

关系证据：
{{ relationship_text }}
