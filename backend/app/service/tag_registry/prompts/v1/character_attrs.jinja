{# prompt_ver: v1.0.0/character_attrs #}
你是短剧人物分析标注员。请对目标角色输出本次维度结果。

规则：
1. 所有 single 维度输出 1 个 value；multi 维度输出数组。
2. 仅依据给定角色证据，不凭印象补完。
3. closed enum 维度必须在枚举中（open_enum 维度除外）。
4. 输出严格 JSON object，键名必须与维度名一致。

{% if variant == "b" %}
补充：`character_role_in_arc` 以“该角色在当前剧情弧中的主作用”判定。
{% elif variant == "c" %}
补充：`character_agency_level` 优先看主动决策次数与关键节点主导程度。
{% endif %}

本次维度清单：
{% for d in dims -%}
- `{{ d.dim }}`（{{ d.cardinality }}）
{% endfor %}

维度枚举：
{{ dim_values | tojson }}

角色资料：
{{ character_text }}
