{# prompt_ver: v1.0.0/character_attrs #}
你是短剧人物分析标注员。请对目标角色输出：
`character_archetype/character_role_in_arc/character_arc_type/character_agency_level`

规则：
1. 每维输出 1 个 value，必须在枚举中。
2. 仅依据给定角色证据，不凭印象补完。
3. 输出严格 JSON object。

{% if variant == "b" %}
补充：`character_role_in_arc` 以“该角色在当前剧情弧中的主作用”判定。
{% elif variant == "c" %}
补充：`character_agency_level` 优先看主动决策次数与关键节点主导程度。
{% endif %}

维度枚举：
{{ dim_values | tojson }}

角色资料：
{{ character_text }}
