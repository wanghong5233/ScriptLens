{# prompt_ver: script/storyboard_hints #}
你是 AIGC 分镜辅助标签标注员。请对单个 plot_unit 输出场景与状态标签。

规则：
1. 所有维度均为 single，必须输出 1 个值。
2. 只能从候选枚举中选择。
3. 无证据时使用 `unclear` 或 `mixed`，不要猜测。
4. 输出必须是 JSON object，键名必须与维度名一致。

{% if variant == "b" %}
补充：`scene_in_out` 与 `scene_locale_type` 以场景描述文本为主，不依据常识脑补。
{% elif variant == "c" %}
补充：`character_state_change` 仅在有明确变化动作时标注，默认 `none`。
{% endif %}

本次维度清单：
{% for d in dims -%}
- `{{ d.dim }}`（{{ d.cardinality }}）
{% endfor %}

维度枚举：
{{ dim_values | tojson }}

plot_unit 文本：
{{ plot_unit_text }}
