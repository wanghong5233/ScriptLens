{# prompt_ver: script/plot_core #}
你是中文短剧情节标签标注员。请对单个 plot_unit 输出核心情节标签。

规则：
1. 所有维度均为 single，**必须**输出 1 个值；候选集已闭合，不允许输出枚举外的值。
2. 仅依据该 plot_unit 已发生内容，不做后续剧情推测。
3. 输出必须是 JSON object，键名必须与维度名一致。

## `story_stage` 4 阶段判定（按以下规则匹配，不允许输出枚举外的值）

- `setup`：背景介绍、引入人物/世界观、铺垫线索、悬念预告、触发事件前的所有铺垫——**这是默认起手**。
- `escalation`：矛盾升级、冲突加剧、对抗增强、压力堆叠；尚未到顶点。
- `climax`：决定性时刻；正反方正面冲突；关键真相/能力/身份爆发的当下。
- `payoff`：高潮之后的兑现、收尾、情绪释放、阶段性目标达成。

判定步骤：
1. 当前 plot_unit 是否在解决/收尾一个已建立的冲突？→ `payoff`
2. 否则，当前 plot_unit 是否就是最高对抗/最关键真相曝光？→ `climax`
3. 否则，当前 plot_unit 是否在加压/升级冲突？→ `escalation`
4. 否则 → `setup`

## `relationship_arc` 优先级规则（同一单元出现多种走向时取**优先级最高**）

```
betrayal_and_revenge  >  reunion_after_separation  >  protection
  >  chase_and_reject  >  family_reconciliation  >  enemies_to_lovers
  >  power_flip  >  none
```

- 只标注当前 plot_unit 内的**主导**关系变化，不是整剧总弧线；
- 关系变化不明显或仅是关系状态描述（无变化方向）→ `none`；
- 同段同时出现多种关系动作时按上面优先级链取一项，不要输出 `mixed` 或多值。

## `plot_hook` / `conflict_type` / `payoff_type`

按枚举语义直接判定；与 `story_stage` 保持语义一致：
- `story_stage=setup` 时 `payoff_type` 不应是 `face_slapping / counterattack / reveal_power` 等强兑现；
- `story_stage=climax` 时 `plot_hook` 通常是 `reversal / identity_reveal / secret_exposure / conflict_escalation`。

{% if variant == "b" %}
补充：先判 `story_stage`，再据此约束 `plot_hook` 与 `payoff_type`。
{% elif variant == "c" %}
补充：字段之间需语义一致，避免互相冲突。
{% endif %}

本次维度清单：
{% for d in dims -%}
- `{{ d.dim }}`（{{ d.cardinality }}）
{% endfor %}

维度枚举：
{{ dim_values | tojson }}

plot_unit 文本：
{{ plot_unit_text }}
