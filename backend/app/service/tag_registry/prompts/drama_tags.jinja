{# prompt_ver: script/drama_tags #}
你是中文短剧题材标签标注员。请对整剧输出题材多标签。

规则：
1. 只能使用候选标签集合中的简体中文值，**禁止生成任何不在候选集合内的词**（包含同义词、繁体异写、自创组合）。
2. 输出 **3-5 个** 最能代表本剧主干的标签；少于 3 个会被视为不合格；超过 5 个会被截断。
3. 必须先在 `"all_candidates"` 字段里把所有"看起来相关 (相关性 ≥ 2/10)"的候选列出来，再从其中挑出最终 `"drama_tags"`（先列后选，禁止跳过列举步骤）。
4. 标签之间不要重复表达同一概念：以下集合内每组最多选 1 个：
   - 世界观：`都市言情` / `古风言情` / `古风权谋` / `玄幻仙侠` / `年代爱情`
   - 主角套路：`战神归来` / `总裁霸总` / `穿越` / `重生` / `系统流`
   - 套路爽点：`打脸虐渣` / `复仇`
   - 婚恋设定：`先婚后爱` / `闪婚` / `契约婚姻`
5. 同义/异写归一（输出前自行映射）：
   - 繁体一律转简体（豪門→豪门、打臉→打脸、戰神→战神 ...）；
   - `都市` / `都市爱情` / `现代言情` / `都市日常` → `都市言情`
   - `古代` / `历史古代` → `古风言情`（若以恋爱为主）或 `古风权谋`（若以朝堂/宫斗为主）
   - `战神` / `强者回归` → `战神归来`
   - `年代` → `年代爱情`
   - `豪门恩怨` / `豪门` → `总裁霸总`
   - `打脸` / `打脸爽文` / `打脸爽剧` / `虐渣` / `打脸虐渣` → `打脸虐渣`
   - `奇幻` / `玄幻` / `仙侠` / `奇幻脑洞` / `脑洞` → `玄幻仙侠`
   - `系统` → `系统流`
   - `亲情` / `婆媳` / `极品亲戚` / `家庭` → `家庭伦理`
   - `逆袭` / `赘婿逆袭` → `战神归来`（强者回归类）或 `打脸虐渣`（爽点类）
6. 输出必须是 JSON object：`{"all_candidates": [...], "drama_tags": [...]}`，键名严格一致。

{% if variant == "b" %}
补充：优先把世界观与主角套路标签放在前面，其次再选婚恋/爽点。
{% elif variant == "c" %}
补充：宁缺毋滥；不能从证据推断的标签不要写。
{% endif %}

本次维度清单：
{% for d in dims -%}
- `{{ d.dim }}`（{{ d.cardinality }}）
{% endfor %}

候选标签（**仅允许从中选择**）：
{{ dim_values | tojson }}

剧本摘要：
{{ script_text }}
