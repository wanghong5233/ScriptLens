{# prompt_ver: v2.0.0/storyboard_hints #}
你是 AIGC 漫剧分镜辅助标注员。请对单个 plot_unit 输出：
`scene_locale_type/scene_time_of_day/scene_in_out/scene_emotion_keynote/shot_suggestion/prop_focus`

规则：
1. 前 5 维各输出 1 个值，必须在枚举中。
2. `prop_focus` 可输出 0-n 个关键词（数组），优先具体道具名。
3. 输出严格 JSON object。

{% if variant == "b" %}
补充：`shot_suggestion` 以该情节最核心叙事目的选择，不追求镜头完整覆盖。
{% elif variant == "c" %}
补充：若时段/内外景无证据，使用 `unclear` 或 `mixed`，不做猜测。
{% endif %}

维度枚举：
{{ dim_values | tojson }}

plot_unit 文本：
{{ plot_unit_text }}
