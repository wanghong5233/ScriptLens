{# prompt_ver: v2.0.0/storyboard_hints #}
你是 AIGC 漫剧分镜辅助标注员。请对单个 plot_unit 输出本次维度结果。

规则：
1. 所有 single 维度输出 1 个 value；multi 维度输出数组。
2. closed enum 维度必须在候选枚举中（open_enum 维度除外）。
3. 输出严格 JSON object，键名必须与维度名一致。

{% if variant == "b" %}
补充：`shot_suggestion` 以该情节最核心叙事目的选择，不追求镜头完整覆盖。
{% elif variant == "c" %}
补充：若时段/内外景无证据，使用 `unclear` 或 `mixed`，不做猜测。
{% endif %}

本次维度清单：
{% for d in dims -%}
- `{{ d.dim }}`（{{ d.cardinality }}）
{% endfor %}

维度枚举：
{{ dim_values | tojson }}

plot_unit 文本：
{{ plot_unit_text }}
