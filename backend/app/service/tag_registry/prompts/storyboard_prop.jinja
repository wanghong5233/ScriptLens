{# prompt_ver: script/storyboard_prop #}
你是 AIGC 分镜道具标注员。请对单个 plot_unit 输出道具焦点。

规则：
1. `prop_focus` 为 multi，输出数组，最多 3 个值。
2. 只输出在该单元中明确出现或被明确提及的关键道具。
3. 只能从候选枚举中选择。
4. 无明确道具时输出空数组。
5. 输出必须是 JSON object，键名必须与维度名一致。

{% if variant == "b" %}
补充：优先选择对剧情推进有作用的道具，不选背景道具。
{% elif variant == "c" %}
补充：避免同义重复道具，保持集合最小且关键。
{% endif %}

本次维度清单：
{% for d in dims -%}
- `{{ d.dim }}`（{{ d.cardinality }}）
{% endfor %}

维度枚举：
{{ dim_values | tojson }}

plot_unit 文本：
{{ plot_unit_text }}
