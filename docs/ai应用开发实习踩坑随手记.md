ai coding 不要在代码里面留下垃圾注释，尤其是历史变更，混淆的版本签名：
    LLM 视角(tool description / schema):只讲当前业务概念,完全不出现版本号
    维护者视角(Python docstring):清晰描述当前用法;真要保留历史背景,放模块顶部一小段就够,函数级 doc 不堆

ai codeing 多模块或者前后端并行开发，或者是plan build模式的开发，容易踩坑，留下很多bug反而需要返工。

每一阶段还是需要细致的验证，不然就是持续埋雷

真实项目开发经验：

    先维护一个稳定的上线版本，增量可控开发，做好git管理，原子提交。方便回溯。

    稳定已有功能胜过加新功能。

    单一事实源直接替换胜过多套兼容方案

    ai coding 老是倾向于能跑，随手散乱写魔法数字，硬编码数字

    数据驱动，状态驱动

    Fail aloud， Fail Fast

